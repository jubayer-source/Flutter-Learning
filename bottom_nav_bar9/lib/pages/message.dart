import 'package:flutter/material.dart';

class UserMassage extends StatelessWidget {
  const UserMassage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Massage Page',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}